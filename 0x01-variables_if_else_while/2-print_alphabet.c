#include <stdio.h>
/**
 * main - prints alphabet in lowercase followed by a new line
 * use only putchar
 * Return: 0 (fo successfully executed code)
 */
int main(void)
{
	char alphabet[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;
		for (i = 0; i < 26; i++)
		{
			putchar(alphabet[]);
		}
	putchar('\n');
	return (0);
}
